{
  "name": "metor-dev-tools",
  "private": true,
  "description": "Metor is a Tor-based messenger framework written in Python.",
  "scripts": {
    "format:md": "prettier --write \"*.md\" \"docs/**/*.md\" \".github/**/*.md\"",
    "format:py": "python -m ruff format src/metor/ scripts/ tests/",
    "fix:py": "python -m ruff check --fix src/metor/ scripts/ tests/",
    "generate:docs": "python scripts/generate_api_docs.py && python scripts/generate_settings_docs.py && python scripts/generate_compatibility_manifest.py",
    "check:md": "prettier --check \"*.md\" \"docs/**/*.md\" \".github/**/*.md\"",
    "check:py": "python -m ruff check src/metor/ scripts/ tests/ && python -m ruff format --check src/metor/ scripts/ tests/",
    "check:types": "python -m mypy src scripts/",
    "check:boundaries": "python scripts/check_boundaries.py",
    "check:generated": "python scripts/validate_generated_docs.py",
    "test": "python scripts/run_tests.py --suite all",
    "check": "npm run check:md && npm run check:py && npm run check:types && npm run check:boundaries && npm run check:generated && npm test",
    "lint": "npm run check:py",
    "typecheck": "npm run check:types",
    "bundle:wheelhouse": "python scripts/build_release_wheelhouse.py",
    "docs": "npm run generate:docs",
    "ready": "npm run check"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DerWahreMirakulix/metor.git"
  },
  "author": "LuRex",
  "license": "GPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/DerWahreMirakulix/metor/issues"
  },
  "homepage": "https://github.com/DerWahreMirakulix/metor#readme",
  "devDependencies": {
    "prettier": "3.8.1"
  }
}
